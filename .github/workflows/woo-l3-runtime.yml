name: Woo L3 Runtime Smoke

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

env:
  WP_CLI_ALLOW_ROOT: '1'
  WP_ROOT: /tmp/aznet-wordpress
  WOO_L3_STATE_DIR: /tmp/woo-l3
  WOO_L3_BASE_URL: http://127.0.0.1:8080

jobs:
  woo-l3-runtime:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: wordpress
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -proot"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=12

    steps:
      - name: Checkout AZnet Theme
        uses: actions/checkout@v4

      - name: Set up PHP 8.1
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'
          extensions: mysqli
          coverage: none

      - name: Verify runtime workflow contract
        run: php tests/offline/woo-l3-runtime-workflow-contract.php

      - name: Install WP-CLI
        run: |
          curl --fail --silent --show-error --location \
            https://raw.githubusercontent.com/wp-cli/builds/gh-pages/phar/wp-cli.phar \
            -o /usr/local/bin/wp
          chmod +x /usr/local/bin/wp
          wp --info

      - name: Build isolated WordPress and WooCommerce runtime
        run: |
          set -euo pipefail
          mkdir -p "$WP_ROOT" "$WOO_L3_STATE_DIR"

          wp core download --path="$WP_ROOT" --force
          WP_VERSION="$(wp core version --path="$WP_ROOT")"
          php -r 'exit(version_compare($argv[1], "6.9", ">=") ? 0 : 1);' "$WP_VERSION"

          wp config create \
            --path="$WP_ROOT" \
            --dbname=wordpress \
            --dbuser=root \
            --dbpass=root \
            --dbhost=127.0.0.1:3306 \
            --skip-check

          wp core install \
            --path="$WP_ROOT" \
            --url="$WOO_L3_BASE_URL" \
            --title="AZnet Theme L3 Runtime" \
            --admin_user=aznet-runtime \
            --admin_password='aznet-runtime-pass' \
            --admin_email=runtime@example.test \
            --skip-email

          wp plugin install woocommerce --activate --path="$WP_ROOT"

          mkdir -p "$WP_ROOT/wp-content/themes/aznet-theme"
          rsync -a "$GITHUB_WORKSPACE/" "$WP_ROOT/wp-content/themes/aznet-theme/" \
            --exclude=.git \
            --exclude=.github

          wp theme activate aznet-theme --path="$WP_ROOT"
          wp theme is-active aznet-theme --path="$WP_ROOT"
          wp plugin is-active woocommerce --path="$WP_ROOT"

          wp eval-file "$GITHUB_WORKSPACE/tests/runtime/woo-l3-fixtures.php" --path="$WP_ROOT"

          {
            echo "WordPress=$WP_VERSION"
            echo "PHP=$(php -r 'echo PHP_VERSION;')"
            echo "WooCommerce=$(wp plugin get woocommerce --field=version --path=\"$WP_ROOT\")"
            echo "Theme=$(wp theme get aznet-theme --field=version --path=\"$WP_ROOT\")"
          } | tee "$WOO_L3_STATE_DIR/runtime-report.txt"

      - name: Start WordPress HTTP runtime
        run: |
          set -euo pipefail
          cd "$WP_ROOT"
          nohup wp server --host=127.0.0.1 --port=8080 >"$WOO_L3_STATE_DIR/wp-server.log" 2>&1 &
          echo $! > "$WOO_L3_STATE_DIR/wp-server.pid"

          for attempt in $(seq 1 30); do
            if curl --fail --silent "$WOO_L3_BASE_URL/" >/dev/null; then
              exit 0
            fi
            sleep 1
          done

          cat "$WOO_L3_STATE_DIR/wp-server.log" >&2 || true
          exit 1

      - name: Run Woo L3 real HTTP smoke
        run: bash "$GITHUB_WORKSPACE/scripts/verify-woo-l3-runtime.sh"

      - name: Upload L3 evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: woo-l3-runtime-evidence
          path: |
            /tmp/woo-l3/runtime-report.txt
            /tmp/woo-l3/runtime-ids.env
            /tmp/woo-l3/wp-server.log
            /tmp/woo-l3/html
          if-no-files-found: ignore
