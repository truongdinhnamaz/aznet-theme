# W8 No-Takeover Boundary

W8 does not wire a Theme renderer into ConvertFlow request resolution and does not take over any ConvertFlow surface. The Theme exposes only presentation custom properties; ConvertFlow decides where and when to consume them on surfaces it owns.
