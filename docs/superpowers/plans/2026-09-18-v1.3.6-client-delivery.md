# AZnet Theme 1.3.6 Client Delivery Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Produce a deterministic, installable AZnet Theme 1.3.6 package from the current canonical Law 01 presentation baseline for client delivery.

**Architecture:** Preserve the current Theme-owned presentation implementation and promote release metadata only. Extend existing Y5/X6 release guards so 1.3.6 is the only newly accepted patch promotion from 1.3.5, then build and verify the deterministic ZIP on WordPress 6.9 / PHP 8.1 with zero active third-party plugins.

**Tech Stack:** WordPress 6.9+, PHP 8.1+, GitHub Actions, PHP contract tests, Playwright/axe, Python deterministic ZIP builder.

**Spec:** `docs/source/AZT-04-roadmap-qa-decisions.md` plus current owner approval to create the latest installable client-delivery package.

## Global Constraints

- Source owns data; Theme owns presentation; integration contracts connect them.
- Do not add provider/domain logic or private storage reads.
- RootProfile Team remains blocked under issue #112.
- Preserve the completed Law 01 visual-parity baseline.
- Release target is a patch promotion from 1.3.5 to 1.3.6.
- Git tag, GitHub Release and production deployment are not implied by package creation.
- Minimum runtime remains WordPress 6.9+ and PHP 8.1+.

---

### Task 1: RED release promotion contract

**Files:**
- Modify: `tests/offline/y5-client-delivery-release-contract.php`
- Modify: `tests/runtime/y5-client-delivery.php`
- Modify: `.github/workflows/y5-client-delivery-release.yml`
- Modify: `tests/offline/x6-promotion-workflow-boundary-contract.php`
- Modify: `.github/workflows/x6-cross-surface-release.yml`

**Interfaces:**
- Consumes: existing 1.3.5 release metadata in `style.css` and `functions.php`.
- Produces: failing release guards that require 1.3.6 and allow only the exact 1.3.5 -> 1.3.6 metadata promotion.

- [ ] **Step 1: Change Y5 release/runtime/workflow expectations to 1.3.6.**
- [ ] **Step 2: Add an X6 1.3.6 promotion boundary accepting only style.css/functions.php version-line changes from 1.3.5 to 1.3.6.**
- [ ] **Step 3: Run CI and verify RED fails because production metadata is still 1.3.5.**
- [ ] **Step 4: Commit the RED release contract.**

### Task 2: GREEN metadata promotion

**Files:**
- Modify: `style.css`
- Modify: `functions.php`

**Interfaces:**
- Consumes: RED 1.3.6 release guards.
- Produces: Theme metadata exactly 1.3.6 in both canonical declarations.

- [ ] **Step 1: Change `Version: 1.3.5` to `Version: 1.3.6`.**
- [ ] **Step 2: Change `AZNET_THEME_VERSION` from `1.3.5` to `1.3.6`.**
- [ ] **Step 3: Run full PR matrix and verify GREEN.**
- [ ] **Step 4: Commit the bounded metadata promotion.**

### Task 3: Client-delivery package and release evidence

**Files:**
- Create: `docs/evidence/V1_3_6_CLIENT_DELIVERY_20260918.md`
- Modify: `docs/source/AZT-03-baseline-provenance.md`
- Modify: `docs/source/AZT-04-roadmap-qa-decisions.md`
- Modify: `docs/source/AZT-EXEC-MAP.md`
- Modify: `docs/source/SOURCE_MANIFEST.md`

**Interfaces:**
- Consumes: exact final 1.3.6 PR head and deterministic Y5 package artifact.
- Produces: source checkpoint identifying the package hash, tested scope, and separate publication/deployment gates.

- [ ] **Step 1: Capture exact-head workflow results and Y5 package artifact metadata.**
- [ ] **Step 2: Record package file count, packaged PHP lint result and SHA-256.**
- [ ] **Step 3: Synchronize authoritative source state to 1.3.6 client-delivery candidate.**
- [ ] **Step 4: Re-run affected documentation/retained workflows.**
- [ ] **Step 5: Stop at merge gate; do not create tag/GitHub Release/deploy without separate approval.**
